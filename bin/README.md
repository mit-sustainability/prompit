Place your local PocketBase binary here as `bin/pocketbase`.

Example:
1. Download PocketBase for your platform.
2. Move/rename binary to `bin/pocketbase`.
3. Make executable: `chmod +x bin/pocketbase`.

Then run local dev stack with:
`npm run dev`
